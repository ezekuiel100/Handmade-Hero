#include <windows.h>


int WINAPI WinMain(
            HINSTANCE hInstance,
            HINSTANCE hPrevInstance,    
            LPSTR     lpCmdLine,
            int       nShowCmd
  ){
    MessageBox(NULL, "This is Handmade Hero", "Handmade hero", MB_OK);
    return 0;
  };
